"""
Faça um algoritmo que lê uma
temperatura em Fahrenheit e
calcula a temperatura
correspondente em Celsius. Ao final
o programa deve exibir as duas
temperaturas.
– Usar a fórmula: C = (5 * (F-32) / 9)
"""
F = float(input("Type the temperature in Fahrenheit"))
C= (5 * (F-32) / 9);
print("Em celsius {}".format(C))
